apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "actor-game.fullname" . }}-test-connection"
  labels:
    {{- include "actor-game.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "actor-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
